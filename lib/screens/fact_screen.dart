import 'package:flutter/material.dart';

class FactScreen extends StatelessWidget {
  const FactScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
    //TODO: Display suggestions - how to stay safe
  }
}
